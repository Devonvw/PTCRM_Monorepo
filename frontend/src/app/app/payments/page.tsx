import PageHeader from "@/components/ui/page-header";

const Payments = () => {
  return (
    <div>
      <PageHeader title="Payments" breadcrumbs={[{ title: "Payments" }]} />
    </div>
  );
};

export default Payments;
